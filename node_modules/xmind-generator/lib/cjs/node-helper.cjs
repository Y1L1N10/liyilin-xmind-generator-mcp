"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.readImageFile = readImageFile;
exports.writeLocalFile = writeLocalFile;
var _path = _interopRequireDefault(require("path"));
function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
async function writeLocalFile(workbook, pathToFile) {
  const {
    writeFile
  } = await Promise.resolve().then(() => require("fs/promises"));
  const buffer = await workbook.archive();
  writeFile(pathToFile, Buffer.from(buffer));
}
async function readImageFile(filePath) {
  const {
    readFile
  } = await Promise.resolve().then(() => require("fs/promises"));
  const filerBuffer = await readFile(filePath);
  return {
    data: filerBuffer,
    name: _path.default.basename(filePath)
  };
}