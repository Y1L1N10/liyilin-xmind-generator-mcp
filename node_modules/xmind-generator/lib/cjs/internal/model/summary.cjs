"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Summary = void 0;
var _common = require("../common.cjs");
class Summary {
  id;
  title;
  from;
  to;
  summaryTopic;
  constructor(title, from, to, summaryTopic) {
    this.id = (0, _common.uuid)();
    this.title = title;
    this.from = from;
    this.to = to;
    this.summaryTopic = summaryTopic;
  }
  isConflictWith(SummaryToCompare) {
    return [this.from, this.to].every(identifier => SummaryToCompare.from === identifier || SummaryToCompare.to === identifier);
  }
}
exports.Summary = Summary;