"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.archive = archive;
exports.asJSONArray = asJSONArray;
exports.asJSONObject = asJSONObject;
exports.serializeRelationship = serializeRelationship;
exports.serializeSheet = serializeSheet;
exports.serializeSummaryInfo = serializeSummaryInfo;
exports.serializeTopic = serializeTopic;
exports.serializeWorkbook = serializeWorkbook;
var _jszip = _interopRequireDefault(require("jszip"));
var _storage = require("./storage.cjs");
function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
function asJSONObject(whatever) {
  if (typeof whatever === "object" && whatever !== null && !Array.isArray(whatever)) {
    return whatever;
  }
  throw new Error(`Not a JSON object: ${JSON.stringify(whatever)}`);
}
function asJSONArray(whatever) {
  if (Array.isArray(whatever)) {
    return whatever;
  }
  throw new Error(`Not a JSON array: ${JSON.stringify(whatever)}`);
}
const resourceIdPrefix = "xap:resources/";
async function serializeWorkbook(workbook, imageResourceSetter) {
  return await Promise.all(workbook.sheets.map(async sheet => serializeSheet(sheet, imageResourceSetter)));
}
async function serializeSheet(sheet, imageResourceSetter) {
  const obj = {
    id: sheet.id,
    class: "sheet",
    title: sheet.title ?? ""
  };
  if (sheet.rootTopic) {
    obj.rootTopic = await serializeTopic(sheet.rootTopic, imageResourceSetter);
  }
  if (sheet.relationships.length > 0) {
    obj.relationships = sheet.relationships.map(relationship => serializeRelationship(relationship));
  }
  return obj;
}
async function serializeTopic(topic, imageResourceSetter) {
  const obj = {
    id: topic.id,
    class: "topic",
    title: topic.title ?? ""
  };
  const {
    note,
    labels,
    image,
    markers,
    summaries
  } = topic;
  if (note) {
    obj.notes = {
      plain: {
        content: note + "\n"
      }
    };
  }
  if (labels.length > 0) {
    obj.labels = [...labels];
  }
  if (topic.children.length > 0) {
    obj.children = {
      attached: await Promise.all(topic.children.map(async child => serializeTopic(child, imageResourceSetter)))
    };
  }
  if (image) {
    const resourcePath = await imageResourceSetter(image);
    if (resourcePath) {
      obj.image = {
        src: resourceIdPrefix + resourcePath
      };
    }
  }
  if (markers.length > 0) {
    obj.markers = markers.map(marker => ({
      markerId: marker.id
    }));
  }
  if (summaries.length > 0) {
    const summaryTopics = [];
    for (const summary of summaries) {
      const serializedSummary = serializeSummaryInfo(topic, summary);
      if (serializedSummary) {
        obj.summaries = [...asJSONArray(obj.summaries ?? []), asJSONObject({
          ...serializedSummary,
          topicId: summary.summaryTopic.id
        })];
        summaryTopics.push(await serializeTopic(summary.summaryTopic, imageResourceSetter));
      }
    }
    if (summaryTopics.length > 0) {
      obj.children = {
        ...asJSONObject(obj.children ?? {}),
        summary: summaryTopics
      };
    }
  }
  return obj;
}
function serializeRelationship(relationship) {
  return {
    id: relationship.id,
    class: "relationship",
    end1Id: relationship.fromTopicId,
    end2Id: relationship.toTopicId,
    title: relationship.title ?? ""
  };
}
function serializeSummaryInfo(topic, summary) {
  const {
    id,
    from,
    to
  } = summary;
  const rangeStart = typeof from === "number" ? from : topic.children.findIndex(child => child.query(from));
  const rangeEnd = typeof to === "number" ? to : topic.children.findIndex(child => child.query(to));
  if (rangeStart < 0 || rangeEnd < 0) {
    return null;
  }
  const range = [rangeStart, rangeEnd].sort();
  return {
    id,
    class: "summary",
    range: `(${range.join(",")})`
  };
}
async function archive(workbook) {
  const zip = new _jszip.default();
  const {
    storage,
    set
  } = (0, _storage.makeImageResourceStorage)();
  const serializedWorkbook = await serializeWorkbook(workbook, set);
  const content = JSON.stringify(serializedWorkbook);
  zip.file("content.json", content);
  const metadata = asJSONObject({
    creator: {
      name: "xmind-generator"
    },
    dataStructureVersion: "2"
  });
  zip.file("metadata.json", JSON.stringify(metadata));
  const resources = zip.folder("resources");
  const resourcePaths = [];
  for (const [path, data] of Object.entries(storage)) {
    resources?.file(path, data);
    resourcePaths.push(`resources/${path}`);
  }
  zip.file("manifest.json", JSON.stringify({
    "file-entries": {
      "content.json": {},
      "metadata.json": {},
      ...Object.fromEntries(new Map(resourcePaths.map(path => [path, {}])))
    }
  }));
  return await zip.generateAsync({
    type: "arraybuffer",
    compression: "STORE"
  });
}