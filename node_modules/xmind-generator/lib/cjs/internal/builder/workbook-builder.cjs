"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.makeWorkbookBuilder = makeWorkbookBuilder;
var _workbook = require("../model/workbook.cjs");
var _types = require("./types.cjs");
function makeWorkbookBuilder(builders) {
  return {
    build() {
      const sheets = builders.map(builder => (0, _types.asBuilder)(builder).build());
      return new _workbook.Workbook(sheets);
    }
  };
}