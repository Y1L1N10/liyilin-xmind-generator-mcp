import { type RefString, Topic } from '../model/topic';
import { type SummaryBuilder, type TopicBuilder } from '../../builder';
import { type Reference } from './ref';
import type { MarkerId } from '../../marker';
import { type NamedResourceData } from '../../storage';
export declare function makeTopicBuilder(title: string): {
    ref(ref: RefString): any;
    children(builders: ReadonlyArray<TopicBuilder>): any;
    image(data: NamedResourceData): any;
    note(newNote: string): any;
    labels(labels: ReadonlyArray<string>): any;
    markers(markers: ReadonlyArray<MarkerId>): any;
    summaries(summaries: ReadonlyArray<SummaryBuilder>): any;
    build(): {
        topic: Topic;
        reference: Reference<Topic>;
    };
};
