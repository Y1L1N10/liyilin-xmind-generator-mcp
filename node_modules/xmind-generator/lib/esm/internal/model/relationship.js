import { uuid } from "../common.js";
export class Relationship {
  id;
  title;
  fromTopicId;
  toTopicId;
  constructor(title, from, to) {
    this.id = uuid();
    this.title = title;
    this.fromTopicId = from;
    this.toTopicId = to;
  }
}
