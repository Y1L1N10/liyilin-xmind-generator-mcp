import { Workbook } from "../model/workbook.js";
import { asBuilder } from "./types.js";
export function makeWorkbookBuilder(builders) {
  return {
    build() {
      const sheets = builders.map((builder) => asBuilder(builder).build());
      return new Workbook(sheets);
    }
  };
}
