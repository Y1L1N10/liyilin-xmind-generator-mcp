export declare function asBuilder<T>(whatever: unknown): Builder<T>;
export interface Builder<T> {
    build: () => T;
}
export type RelationshipInfo = {
    title: string;
    from: string;
    to: string;
};
export type SummaryInfo = {
    title: string;
    from: string | number;
    to: string | number;
};
